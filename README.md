# bundle

> **This branch is generated output — do not hand-edit.**

It is assembled automatically by `scripts/build-bundle.mjs` in the superproject.
To rebuild and publish, run from the superproject root:

```
node scripts/build-bundle.mjs --push
```
